on:
  pull_request:
  push:
    branches:
      - 2.x
name: CI
env:
  SYMFONY_DEPRECATIONS_HELPER: disabled
  BROWSERTEST_OUTPUT_DIRECTORY: 'sites/simpletest'
  COMPOSER_MIRROR_PATH_REPOS: 0
jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.2']
        core: ['^10.1']
        include:
          - core: '^9.5'
            php-version: '8.1'
    container:
      image: ghcr.io/tuutti/drupal-php-docker:${{ matrix.php-version }}

    services:
      artemis:
        image: quay.io/artemiscloud/activemq-artemis-broker
        env:
          AMQ_EXTRA_ARGS: "--nio --user admin --password admin"
      db:
        image: mariadb
        env:
          MYSQL_USER: drupal
          MYSQL_PASSWORD: drupal
          MYSQL_DATABASE: drupal
          MYSQL_ROOT_PASSWORD: drupal
        ports:
          - 3306:3306

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set target core
        run: echo "_TARGET_CORE=${{ matrix.core }}" >> $GITHUB_ENV

      - name: Set Drupal root
        run: echo "DRUPAL_ROOT=$HOME/drupal" >> $GITHUB_ENV

      - name: Create Drupal root
        run: mkdir $DRUPAL_ROOT

      - name: Parse $MODULE_NAME from composer.json
        run: echo "MODULE_NAME=$(cat composer.json | jq -r .name | awk -F/ '{print $NF}')" >> $GITHUB_ENV

      - name: Set module folder
        run: echo "MODULE_FOLDER=web/modules/custom/$MODULE_NAME" >> $GITHUB_ENV

      - name: Merge composer.json files
        run: |
          curl https://git.drupalcode.org/project/gitlab_templates/-/raw/1.0.x/scripts/expand_composer_json.php > expand_composer_json
          php expand_composer_json $MODULE_NAME
          cat composer.json
          cp composer.json $DRUPAL_ROOT

      - name: Setup composer and dependencies
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: composer install

      - name: Symlink module
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: |
          ls -la .
          ls -la web/modules/
          ls -la web/modules/custom
          ln -s $GITHUB_WORKSPACE $MODULE_FOLDER

      - name: Run PHPStan
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: |
          vendor/bin/phpstan analyze -c $MODULE_FOLDER/phpstan.neon $MODULE_FOLDER

      - name: Run PHPCS
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: |
          vendor/bin/phpcs $MODULE_FOLDER --standard=Drupal --extensions=php,module,install

      - name: Install Drupal
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: |
          php -d sendmail_path=$(which true); vendor/bin/drush --yes -v site-install minimal --db-url="$SIMPLETEST_DB"
          vendor/bin/drush en $MODULE_NAME -y

      - name: Start services
        working-directory: ${{ env.DRUPAL_ROOT }}
        run: |
          vendor/bin/drush runserver $SIMPLETEST_BASE_URL --dns > /dev/null 2>&1 &
          # Wait for Drush server to start.
          for i in {1..5}; do
            RESPONSE_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$SIMPLETEST_BASE_URL" || true);
            if [ "$RESPONSE_CODE" -gt "301" ] || [ "$RESPONSE_CODE" -lt "200" ]; then
              sleep 2;
            fi
          done

      - name: Run PHPUnit tests
        working-directory: ${{ env.MODULE_FOLDER }}
        run: phpunit -c tests/phpunit.xml .

      - name: Upload test coverage
        working-directory: ${{ env.MODULE_FOLDER }}
        run: codecov
